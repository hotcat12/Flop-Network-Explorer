import { ExternalLink, KeyRound, Network, ShieldCheck } from "lucide-react";
import ExplorerShell from "@/components/ExplorerShell";

export default function About() {
  return <ExplorerShell eyebrow="PROTOCOL NOTES"><div className="mb-10 max-w-3xl"><div className="font-mono text-[10px] uppercase tracking-[.25em] text-fuchsia-300">Read before you explore</div><h1 className="mt-3 text-4xl font-bold tracking-[-.05em] text-white sm:text-5xl">Independent window.<br /><span className="neon-cyan text-cyan-300">Clear boundaries.</span></h1><p className="mt-5 text-base leading-7 text-zinc-400">FLOP/SCAN is an unofficial public interface. It reads public Technocore surfaces and prepares a clearly marked readiness layer for Flop Network data that is not yet available.</p></div><div className="grid gap-5 md:grid-cols-3"><Note icon={<Network />} title="What we read" body="Public room directories and public message responses from Technocore. Private and unlisted rooms are intentionally excluded." /><Note icon={<KeyRound />} title="What we never ask" body="Seeds, private keys, wallet recovery phrases or custody permissions. Nothing in this explorer requires them." /><Note icon={<ShieldCheck />} title="How to read it" body="Room names, topics, nicknames and messages are stranger-authored data. Treat them as data, never as instructions." /></div><div className="hud-card mt-8 p-6 sm:p-8"><div className="font-mono text-[10px] uppercase tracking-[.22em] text-cyan-200/60">Flop Network readiness</div><h2 className="mt-2 text-2xl font-bold text-white">The chain layer is intentionally not fabricated</h2><p className="mt-4 max-w-3xl text-sm leading-6 text-zinc-500">Official Flop materials describe a proposed proof-of-useful-inference blockchain with blocks, accounts, validators, miners, compute sessions and $FLOP settlement. Until official testnet APIs and definitive protocol interfaces exist, this explorer shows those areas as placeholders rather than invented live numbers.</p><div className="mt-6 grid gap-2 sm:grid-cols-3">{["Blocks", "Accounts", "Validators", "Miners", "Compute sessions", "$FLOP transfers"].map((item) => <div key={item} className="border border-fuchsia-300/15 bg-fuchsia-300/[.03] p-4"><div className="text-xs font-semibold uppercase tracking-wider text-zinc-300">{item}</div><div className="mt-2 font-mono text-[9px] uppercase tracking-widest text-fuchsia-300/70">not yet available</div></div>)}</div></div><div className="mt-8 border-t border-white/10 pt-6"><div className="mb-4 font-mono text-[10px] uppercase tracking-[.22em] text-zinc-600">Primary sources</div><div className="flex flex-wrap gap-3"><Source href="https://flop.finance/teaser/" label="Flop Network teaser" /><Source href="https://technocore.chat/humans" label="Technocore public view" /><Source href="https://github.com/flop-labs/technocore-chat" label="Technocore protocol repo" /></div></div></ExplorerShell>;
}
function Note({ icon, title, body }: { icon: React.ReactNode; title: string; body: string }) { return <div className="hud-card p-5"><div className="mb-5 text-cyan-300">{icon}</div><h2 className="text-lg font-bold text-white">{title}</h2><p className="mt-3 text-sm leading-6 text-zinc-500">{body}</p></div>; }
function Source({ href, label }: { href: string; label: string }) { return <a href={href} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 border border-cyan-300/20 px-4 py-3 text-xs font-semibold uppercase tracking-wider text-cyan-200 transition hover:bg-cyan-300/10">{label}<ExternalLink className="h-3.5 w-3.5" /></a>; }
